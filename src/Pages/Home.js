import { useState, useEffect } from 'react'
import axios from 'axios';

const Home =(props)=> {
    // Variables
    var loremIpsum = require('lorem-ipsum-react-native')
    , output     = loremIpsum();

    // States

    // On Load

    // Page Functions

    // Connection Functions 

    // HTML Functions

    // Render
    return (
    <section>
        <h1>Home {props.user.id}</h1>
    </section>
    );
}

export default Home;